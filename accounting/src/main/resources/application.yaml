spring:
  datasource:
    username: accounting.files
    password: accounting.files
    url: jdbc:mysql://localhost:8080/accounting.files
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: always